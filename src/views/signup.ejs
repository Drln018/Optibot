<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Usuario</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/emailjs-com@2.6.4/dist/email.min.js"></script>
</head>
<body class="bg-blue-400 flex items-center justify-center h-screen" style="background-color: lightskyblue">
    <div class="bg-white w-96 p-8 rounded-lg shadow-lg">
        <div style="text-align: center;">
            <img src="img/registro.png" alt="Logo" class="mx-auto my-4 w-32 h-auto">
        </div>     
        <p class="block text-gray-700 font-semibold mb-2 text-center">CREAR CUENTA</p>       
      
        <% if (message) { %>
            <div class="alert alert-danger"><%= message %></div>
        <% } %>
         

        <form action="/signup" method="post">
            <div>
                <label class="block text-gray-700 font-semibold mb-2" for="email">Email</label>
                <input class="w-full p-3 mb-4 rounded-lg bg-blue-400 text-white focus:outline-none placeholder-white" type="email" id="email" name="email" placeholder="Email" required>
            </div>
            <div class="relative mb-4">
                <label class="block text-gray-700 font-semibold mb-2" for="password">Password</label>
                <input class="w-full p-3 rounded-lg bg-blue-400 text-white focus:outline-none placeholder-white" type="password" id="password" name="password" placeholder="Password" required>
                <i class="fas fa-eye absolute right-3 top-3 text-white"></i>
            </div>
            
            <!-- Botón de enviar -->
            <button type="submit" class="w-full p-3 mb-4 bg-gray-200 text-gray-800 font-semibold rounded-lg shadow-md hover:bg-gray-300 focus:outline-none">Registrar</button>
            
            <div class="block text-gray-700 font-semibold mb-2 text-center">
                <p>¿Ya tienes una cuenta? <a href="/login" class="underline text-blue-500">Login</a></p>
            </div>
        </form>        
    </div>
</body>
</html>
